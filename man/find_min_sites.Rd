% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PowerAfterFunctions.r, R/temp2.r
\name{find_min_sites}
\alias{find_min_sites}
\title{find_min_sites}
\usage{
find_min_sites(
  nB,
  nA,
  delta,
  sd_w,
  sd_d = 0,
  target_power = 0.8,
  alpha = 0.05,
  S_grid = 2:50,
  nsim = 2000,
  seed = 1,
  distribution = c("normal", "nbinom", "binomial"),
  useTest = c("paired-t", "wilcoxon", "prop.test"),
  nbinom_mu = NULL,
  nbinom_disp = NULL,
  binomial_size = NULL,
  binomial_prob = NULL
)

find_min_sites(
  nB,
  nA,
  delta,
  sd_w,
  sd_d = 0,
  target_power = 0.8,
  alpha = 0.05,
  S_grid = 2:50,
  nsim = 2000,
  seed = 1,
  distribution = c("normal", "nbinom", "binomial"),
  useTest = c("paired-t", "wilcoxon", "prop.test"),
  nbinom_mu = NULL,
  nbinom_disp = NULL,
  binomial_size = NULL,
  binomial_prob = NULL
)
}
\arguments{
\item{nB}{Number of before measurements per site}

\item{nA}{Number of after measurements per site}

\item{delta}{Hypothesized mean change}

\item{sd_w}{Within-site standard deviation}

\item{sd_d}{Between-site standard deviation of true changes}

\item{target_power}{Desired power level to achieve}

\item{alpha}{Significance level, defaults to 0.05}

\item{S_grid}{Grid of site numbers to evaluate}

\item{nsim}{Number of simulations to run}

\item{seed}{Random seed for reproducibility}

\item{distribution}{Distribution for simulated data. One of
"normal", "nbinom", or "binomial".}

\item{useTest}{Which test to use. One of "paired-t", "wilcoxon", or "prop.test".}

\item{nbinom_mu}{Mean parameter for negative binomial (mu)}

\item{nbinom_disp}{Dispersion (size) parameter for negative binomial}

\item{binomial_size}{Size parameter (trials) for binomial}

\item{binomial_prob}{Probability parameter for binomial}
}
\value{
A list containing the minimum number of sites needed to achieve the target power and a data frame of the power curve

A list containing the minimum number of sites needed to achieve the target power and a data frame of the power curve
}
\description{
Function to find the minimum number of sites needed to achieve target power from
a paired t test on the means across observations at sites before and after a changepoint.

Function to find the minimum number of sites needed to achieve target power from
a paired t test on the means across observations at sites before and after a changepoint.
}
